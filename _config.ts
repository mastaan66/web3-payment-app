/**
 * Centralized app settings, constants, environment configs, SEO defults; 
 * These values can be imported in the APP whenever needed;
 */

const config = {
  metadata: {
    title: 'Web3 Payments App',
    description: 'Block chain based payments application',
    keywords: 'Web3 Blockchain Payments'
  },
  server: {
    host: 'localhost',
    port: 3000
  }
};

export default config;
