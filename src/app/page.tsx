import React from 'react';

export default function HomePage() {
  return (
    <main className='flex h-full w-full flex-col items-center justify-center'>
    </main>
  );
}

/**
 * When Sending Money
 * First I Will take the address of the receiver or qr code of the receiver.
 * Send the money to the receiver.
 * Record the transaction.
 * 
 * When Receiving Money
 * I'll send my addredd or qr of address and transaction details
 * verify transaction and record the transaction.
 */
